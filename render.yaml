services:
  - type: web
    name: precision-wd-site
    runtime: node
    plan: starter
    buildCommand: npm ci
    startCommand: node server.js
    envVars:
      - key: SQUARE_ACCESS_TOKEN
        sync: false
      - key: SQUARE_ENV
        value: production
      - key: SQUARE_LOCATION_ID
        sync: false
